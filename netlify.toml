[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Proxy /api requests to the Render backend to avoid CORS issues
[[redirects]]
  from = "/api/*"
  to = "https://book-yard.onrender.com/api/:splat"
  status = 200
  force = true
